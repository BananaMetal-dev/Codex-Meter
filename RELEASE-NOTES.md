# v1.0.0

Codexの使用量・リセット時刻・リセットチケット期限を確認するWindows用ミニアプリです。

- Windows 10 / 11 x64向け。必要な.NETランタイムを同梱しています。
- 公式Codex CLIがインストールされ、CodexでChatGPTアカウントの認証が利用できることが前提です。
- `CodexMeter-Setup.exe` をダウンロードして実行してください。
- スタートメニューの `Codex + Meter` で連携起動、`Codex Meter` で単体起動できます。
- コード署名を行っていないため、初回起動時にWindows SmartScreen等の警告が表示される場合があります。
- 使用量履歴、テレメトリ、チケット自動使用、アプリ内自動アップデートはありません。
- 更新はMeterを終了してから同じ場所へインストールしてください。通知済み状態は保持されます。

`SHA256SUMS.txt` でインストーラーのSHA-256を確認できます。
